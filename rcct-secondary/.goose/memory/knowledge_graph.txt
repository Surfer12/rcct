# knowledge_graph structure app docs rcct
Project knowledge graph created and saved to project_knowledge_graph.json, representing the structure of the RCCT project with app and docs components. The graph identifies empty directories in a standard Java project structure with documentation components.

